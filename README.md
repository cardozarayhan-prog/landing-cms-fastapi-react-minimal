# landing-cms-fastapi-react-minimal
Minimal FastAPI demo powering a landing page with editable sections and a contact form. Includes JSON-backed API (list/get/upsert), static admin UI (no DB), background mock contact sender, basic tests, and a README with steps to run locally and extend to Postgres, Docker, or auth.
